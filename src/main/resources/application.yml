spring:
  application:
    name: back

  datasource:
    url: jdbc:h2:./db_dev;MODE=MySQL
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create # 테이블 변경사항 자동으로 반영
    show-sql: true

server:
  port: 8070